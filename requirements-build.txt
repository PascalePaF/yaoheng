-r requirements.txt

# Complete build-only dependency lock for Windows packaging and icon generation.
# The runtime lock above and every build transitive dependency are explicit.
altgraph==0.17.5
packaging==26.3
pefile==2024.8.26
Pillow==12.3.0
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.6
pywin32-ctypes==0.2.3
setuptools==84.0.0
