# Complete runtime dependency lock for Python 3.11 or newer.
# Install with --no-deps so an undeclared transitive dependency fails closed.
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.19
requests==2.34.2
tzdata==2026.3
urllib3==2.7.0
