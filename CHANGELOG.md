# 更新记录

## 未发布

## 3.21.0

- 将设置页外观主题改为可展开/收回的色块画廊：收回时显示当前主题，展开后每套主题独立显示五段矩形色块、名称与配色来源，支持鼠标和键盘选择。
- 在设置页最下方新增“切换下一个主题”按钮，按画廊顺序循环切换，并同步显示当前主题与下一套主题。
- 重制 28 套深色主题，参考 Catppuccin、Nord、Everforest、Rosé Pine、Tokyo Night、Gruvbox 和 Solarized 的公开调色板；保留可识别的色相层次，同时限制大面积亮度、强调色刺激度和文字对比。
- 保留全部旧主题内部键与设置兼容性，覆盖升级后不会重置用户选择；主题切换继续保持在 100 ms 内，不改变界面布局和字号。
- Windows 安装器先在独立临时目录完成资源写入和封装，再原子发布到 Release 目录，避免安全软件扫描新 EXE 时触发 Inno Setup `EndUpdateResource` 文件占用失败。
- 自动化回归测试增至 345 项，新增主题来源完整性、调色板差异与刺激度、画廊收展、真实色块绘制、选择同步和顺序循环覆盖。

## 3.20.2

- 重构 GitHub 更新请求：检查、元数据、校验文件及安装包传输遇到超时、连接重置、限流或 GitHub 5xx 时会进行有限退避重试；中断的安装包从头原子重下，不会留下或误用半成品。
- 更新错误会区分 GitHub 404、限流、服务异常、安全连接与本机代理故障，并只显示脱敏后的代理主机和端口；下载地址、附件大小、正式 Release 与 SHA-256 的原有安全校验保持严格失败关闭。
- 新增 Windows 单实例保护：曜衡同时只保留一个进程窗口，重复启动会通知既有窗口恢复、置前并获得焦点，不会再打开第二份界面。
- 明确设置页关闭按钮行为，可选择“最小化到任务栏”或“彻底退出软件”；安装升级及“立即退出”仍会可靠结束进程。
- 将 28 套主题全部重制为低饱和深色方案，使用分层深色表面、柔和强调色和稳定文字对比；合并控件树遍历并缓存同类颜色属性，完整主题切换稳定在 100 ms 内，同时保留旧主题内部键以免升级后重置选择。
- 自动化回归测试增至 340 项，新增网络重试/断流恢复/代理脱敏、真实 Windows 命名互斥、关闭行为、窗口恢复与全主题暗色约束；并通过真实 GitHub Release 更新链路烟测。

## 3.20.1

- 修复 C2C 兑换页编辑下方六个金额框时，重新换算或异步报价会销毁并重建输入卡片造成的闪烁、卡顿和焦点跳动；现在所有报价、状态和样式均原地更新。
- 兼容 Binance 官方 P2P Skill 当前广告字段 `minTransAmount`、`maxTransAmount`、`tradableAmount`，并兼容 0–1 与 0–100 两种完成率表示；真实在线金额匹配烟测通过。
- 主题扩展为 28 套低刺激深浅配色，以柔和大面积底色、克制饱和度和可读对比为约束；设置页改为可搜索选择并保留即时切换，完整界面全部主题均低于 100 ms。
- 计算器进入页面后自动聚焦公式框，补齐数字小键盘 0–9 与运算符输入，同时保留主键盘、全角输入、回车与等号求值。
- 自动化回归测试增至 324 项，新增真实接口结构、输入控件身份稳定、全主题对比度及数字键盘路径覆盖。

## 3.20

- 将原七币种页面固定为独立“C2C 兑换”，并新增完全分开的“市场兑换”页面；普通公开行情不再混入 C2C 结果或冒充可执行购买价。
- 七个金额框全部支持输入数额或加减乘除、除余与括号算式；最后编辑的币种成为当前输入源，并同步换算另外六项，主币仍独占首行。
- 法币与虚拟币按实际输入金额查询单广告 C2C；虚拟币之间通过用户选择的结算法币执行“卖出源币、再买入目标币”的双段 C2C 参考路径，两段均禁止普通行情降级。
- C2C 控制区可选择自动、Binance、OKX、跨币结算法币及平台返回的支付方式；Binance 使用官方公开只读接口，OKX 继续在没有官方商家授权时明确显示未配置。
- C2C 兑换、市场兑换、货币和虚拟币页面分别持久化上次币种、当前输入与金额，升级后不再重置为默认组合；V3.19 的七币设置会安全迁移到两个新页面。
- 设置页新增深夜橙、明亮橙、深海蓝、森林绿和暮色紫五套高对比度主题；完整界面实测约 50–60 ms 完成切换，布局和字号保持不变，保存过程不阻塞重绘。
- 自动化回归测试增至 318 项，覆盖七栏任意输入、C2C/市场隔离、虚拟币双段 C2C、状态迁移、多主题完整性及真实 Tk 生命周期。

## 3.19

- 兑换页七个币种框统一改为与货币/虚拟币页相同的搜索选择器：可点击输入搜索、使用上下键移动候选并按回车确认。
- 主币卡片改为独占第一整行并左对齐，六个目标币继续按三列两行排列，长算式输入与结果阅读更清晰。
- 主币数额支持加减乘除、除余和括号算式；使用十进制计算，按回车、输入等号或离开输入框后求值，避免 `0.1 + 0.2` 一类浮点误差。
- 设置页新增 GitHub 正式 Release 更新检查：只接受曜衡仓库的固定安装包，下载前后校验地址、大小、Release 摘要和 `SHA256SUMS.txt`，临时文件使用原子替换。
- 安装包支持覆盖旧版本：沿用相同应用标识和原安装目录，升级时关闭并可重启曜衡，替换旧运行文件，同时保留设置、收藏、历史、缓存及本机 API 校验材料。
- 自动化回归测试增至 310 项，覆盖兑换交互、算式安全边界、更新元数据、下载校验与覆盖升级配置。

## 3.18

- 新增独立“兑换”页：一个主币居中、六个目标币按三列两行排列，可在七个法币或虚拟币之间同步换算，并支持设为主币和使用未舍入精确结果互换。
- 七币兑换页与虚拟币三端页都可选择“普通汇率”或“C2C 按金额”，普通边继续使用十进制精确换算，法币与虚拟币边才会请求 C2C 报价。
- 新增 Binance 官方 P2P 只读报价与广告列表适配；支付方式先从平台返回的官方标识中选择，不硬编码付款标识，不包含账户、下单、订单或商家管理能力。
- 新增 OKX 官方白名单 P2P 只读接入框架；默认未配置且不会进入自动报价，只有取得官方授权、字段契约与瞬时凭据后才可启用。
- C2C 结果区分最低展示价、本金额匹配价、普通行情回退、实时/缓存/过期/不可用状态，并明确不保证账户资格或实际成交。
- 货币、虚拟币、行情与七币兑换页保存上次币种、金额、主输入、计价币、周期和 C2C 筛选；设置采用 schema v2、原子写入、备份恢复与旧版本迁移保护。
- 新增默认关闭的本机 API 与机器人命令层，仅监听 `127.0.0.1`；除健康检查外使用 Bearer 认证，令牌明文只在生成或轮换时显示一次，磁盘仅保存 scrypt 校验材料。
- 修复七币/三端异步报价旧结果覆盖、支付方式跨法币串用、部分快照覆盖历史币种、非法输入未使旧回调失效、隐藏页面继续发起 C2C 等竞态。
- 构建与卸载流程覆盖新增模块和本机 API 私密目录；发布包拒绝设置、缓存、令牌、迁移备份、本机路径与常见凭据特征，资产名统一为稳定 ASCII。
- 自动化回归测试增至 298 项，并完成真实 Tk 窗口生命周期、本机 API 启停/健康检查、隔离构建依赖与发布隐私检查。

## 3.17

- 修复隐式乘法下正负号破坏表达式、全角公式连续操作、三角函数象限浮点噪声及非零结果静默下溢，并加入固定种子属性与状态机测试。
- 加固行情响应体、时间戳、USD 基准、图表标识、极端数值和缓存写入校验；同范围刷新复用在途请求，并阻止旧响应覆盖较新行情。
- 修复快速切换行情时旧图表错配、所选时区显示、全角金额、缺失涨跌误着色、无数据排序和下拉残留；加强 Tk 回调恢复、DPI 与主题可读性。
- 设置保存对同一进程内的多实例共享文件锁，降低并发写入造成设置或备份损坏的风险。
- Release 安装器与绿色 ZIP 改用稳定 ASCII 附件名，避免托管平台剥离中文字符；构建会清理旧版本及旧中文发布资产，并在打包前校验名称。
- 构建新增暂存目录、ZIP 内容一致性、路径穿越、隐私文件名、构建机路径与常见秘密特征检查，失败时不再遗留可误认的新发布资产。
- 为安装器和绿色包收集实际捆绑组件的许可证文本，并生成覆盖安装器与绿色 ZIP 的 `SHA256SUMS.txt`。
- CI 更新为固定提交的当前 GitHub Actions，校验 PowerShell 语法，并拒绝未锁定的隐式依赖和源码包安装。
- 发布构建要求 Python 3.13.15+ 并检查捆绑 OpenSSL 的安全修复基线，阻止旧解释器把已知脆弱原生库带入产物；CI 同时覆盖 3.11 源码兼容性与 3.13 发布运行时。
- 明确卸载器只删除应用目录内经用户确认的数据，自定义数据目录始终保留。
- 自动化回归测试增至 147 项，并完成隔离依赖安装、真实绿色包与 Windows 安装器构建、许可证收集、隐私扫描和 SHA-256 验证。

## 3.16

- 新增 64 位 Windows 当前用户安装包，无需管理员权限，提供开始菜单入口、可选桌面快捷方式和标准卸载流程。
- 安装器支持简体中文与英文；卸载时可选择保留或移除本机设置、计算历史和行情缓存，静默卸载默认保留数据。
- 将绿色版 ZIP 与安装版统一纳入隔离构建和隐私检查，发布时同时提供 SHA-256 校验值。
- 重写项目 README，补齐下载、功能、系统要求、隐私、风险提示、源码运行、测试、构建和许可说明。
- 公开发布前检查当前文件与完整 Git 历史，未发现令牌、密码、私钥、个人缓存或本机绝对路径。

## 3.15

- 修复法币行情列表只有双击所选币种后才显示涨跌和颜色的问题，改为复用批量 24 小时快照即时更新整表。
- 按当前计价货币计算交叉汇率涨跌；切换计价币时同步刷新价格、方向、颜色和详情趋势。
- 区分列表 24 小时变化与详情图所选周期，并完善持平、缺失数据、离线回退及异步旧结果处理。
- 回归测试增至 106 项，并完成真实法币整表与计价币切换联网验收。

## 3.14

- 重构计算器输入与连续运算状态，修复精度、科学计数法、负操作数、平方根、百分比转换及输入规范化边界。
- 加固法币、虚拟币、历史趋势和缓存数据的校验、精度、并发合并、故障回退与原子落盘。
- 将所有后台结果安全转交界面主线程，处理快速切换、刷新排队、窗口关闭与延迟回调竞态。
- 设置文件增加原子保存、有效备份和损坏恢复，并完善数据目录、窗口位置及时区性能。
- 拆分并锁定运行/构建依赖，使用隔离构建环境，收紧 GitHub Actions 权限与版本来源。
- 回归测试增至 97 项，并完成 50,000 次随机计算验收、真实行情联网验证、绿色版启动及隐私扫描。

## 3.13

- 修复科学计数法、负数操作数、除余连续输入、括号补全和大整数精度问题。
- 提升结果与金融小数的有效数字显示，优化趋势图绘制性能。
- 限定缓存管理范围，避免影响用户自定义目录中的其他文件。
- 增强汇率缓存、网络返回值和设置文件校验，区分成功、部分更新与失败状态。
- 构建过程改为隔离打包，保留本机用户数据并生成不含个人数据的绿色 ZIP。
- 自动化回归测试由 23 项扩展到 37 项，并加入 GitHub Actions。

## 3.12 baseline

- 迁移前的原始版本，保留为 Git 标签 `v3.12-baseline`。
